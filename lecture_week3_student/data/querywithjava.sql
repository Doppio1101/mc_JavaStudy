SELECT LAST_NAME, FIRST_NAME
FROM EMPLOYEES;
-- employees의 LAST_NAME, FIRST_NAME을 출력.
-- LAST_NAME이 먼저 나오도록

SELECT useID, amount
FROM buyTBL
ORDER BY useID;

SELECT useID, SUM(amount)
FROM buyTBL
GROUP BY useID;

CREATE TABLE testTBL2
(	id NUMBER(4) PRIMARY KEY,
	userNAME NCHAR(3) NOT NULL,
	age NUMBER(2),
	nation NCHAR(4) DEFAULT '대한민국'
);

CREATE SEQUENCE idSEQ
	START WITH 1
	INCREMENT BY 1;
DROP SEQUENCE idSEQ;
INSERT INTO testTBL2 VALUES(idSEQ.NEXTVAL, '유나', 25, default);
SELECT * FROM testTBL2;
SELECT IDSEQ.CURRVAL FROM DUAL;
SELECT 100*100 FROM DUAL;
--querywithjava